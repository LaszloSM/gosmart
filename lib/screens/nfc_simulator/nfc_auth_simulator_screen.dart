// lib/screens/nfc_simulator/nfc_auth_simulator_screen.dart
// Placeholder — full implementation in Task 16
import 'package:flutter/material.dart';

class NfcAuthSimulatorScreen extends StatelessWidget {
  const NfcAuthSimulatorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('NFC Simulator — coming soon')),
    );
  }
}
